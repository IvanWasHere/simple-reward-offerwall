/** Fixed decorative background layers (glow + noise) from the prototype. */
export function Backgrounds() {
  return (
    <>
      <div className="bg-glow" />
      <div className="bg-noise" />
    </>
  );
}
